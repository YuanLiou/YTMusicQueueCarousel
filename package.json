{
  "name": "youtube-music-queue-carousel",
  "version": "0.1.0",
  "private": true,
  "description": "A Chrome Manifest V3 carousel interface for the current YouTube Music queue.",
  "scripts": {
    "test": "node --test"
  }
}
